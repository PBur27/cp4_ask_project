name: HelloWorld
on: [push]

jobs:
  Hello-World:
    runs-on: ubuntu-latest
    steps:
      - name: "First Command"
        run: echo "Pawel"
      - run: echo "The name of this branch is ${{ github.ref }}"
      - uses: actions/checkout@v4
      - name: "show structure"
        run: tree ./
      - run: echo "it works"
      - run: mkdir /foo/moo/boo
    
